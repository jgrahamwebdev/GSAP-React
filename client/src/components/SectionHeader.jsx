
const SectionHeader = ({ title }) => {
    return (
        <h6 className="mb-[50px] uppercase font-bold font-syncopate tracking-[1px] text-[#5f5f5f]">{title}</h6>
    )
}

export default SectionHeader
